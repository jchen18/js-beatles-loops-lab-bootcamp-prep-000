function theBeatlesPlay(musicians,instruments){
  var pairing = [];
  for (let i = 0; i < musicians.length; i++){
    var specific_musician = alert(musician[i])
    pairing.push("${i} plays ${i}")
  }
 return pairing 
}